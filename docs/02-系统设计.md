# 系统设计

## 1. 技术选型

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 框架 | Next.js 14（App Router, TypeScript） | 前后端一体，单进程部署 |
| UI | React 18 + Tailwind CSS | 服务端组件渲染列表，客户端组件处理表单交互 |
| 数据 | Prisma ORM + SQLite | 单文件数据库，便于备份迁移 |
| 鉴权 | 自实现 HMAC 签名 Cookie | 不引入额外鉴权依赖，兼容 Edge 中间件 |

选型理由：场景为小型内部运营工具，单人/小团队使用，数据量小。Next.js 一体化方案开发快、部署简单（一个 `npm run start`），SQLite 免去额外数据库运维。

## 2. 整体架构

```
浏览器
  ├── 首页 /               ──> 重定向到 /admin（需登录）
  ├── 公开页 /view         ──(服务端组件直接读库)──┐
  ├── 登录页 /login        ──> POST /api/auth/login │
  └── 后台 /admin/**       ──> /api/admin/**         │
                                                     ▼
                          middleware.ts (校验会话)  Prisma  ──> SQLite (dev.db / prod.db)
```

- **首页 `/`**：重定向到 `/admin`，未登录会被中间件转到 `/login`，不对外展示数据。
- **公开页 `/view`**：服务端组件（`src/app/view/page.tsx`）直接通过 Prisma 读库渲染，对金额脱敏，**不含后台入口**，可单独分享给客户。
- **管理页**：服务端组件渲染数据，表单交互由客户端组件通过 `fetch` 调用 `/api/admin/**`。
- **中间件**：`src/middleware.ts` 拦截 `/admin/**` 与 `/api/admin/**`，校验签名 Cookie；页面未授权重定向 `/login`，API 返回 401。

## 3. 目录结构

```
src/
├── lib/
│   ├── db.ts          # PrismaClient 单例
│   ├── dates.ts       # 剩余天数 / 到期状态 / 日期格式化
│   ├── auth.ts        # 密码校验 + HMAC 签名/验签会话
│   └── validate.ts    # 请求体字段归一化与校验
├── middleware.ts      # 路由鉴权（Edge 运行时）
└── app/
    ├── layout.tsx                 # 根布局（含深色模式防闪烁脚本）
    ├── globals.css                # Tailwind 入口 + 可复用组件样式（含明暗两套）
    ├── ThemeToggle.tsx            # 明暗切换按钮（客户端）
    ├── admin/ImageUpload.tsx      # 付款截图上传/预览（客户端，复用于 VPS/续费表单）
    ├── page.tsx                   # 默认首页：重定向到 /admin（需登录）
    ├── view/page.tsx              # 裸 /view → 重定向到 /login（全局公开页已移除）
    ├── view/[customerId]/page.tsx # 客户专属公开页（含购买成本/实付/剩余时间及合计）
    ├── login/page.tsx             # 登录页（客户端组件）
    ├── admin/
    │   ├── layout.tsx             # 后台导航（VPS/客户/提供商）+ 退出
    │   ├── LogoutButton.tsx       # 退出按钮（客户端）
    │   ├── page.tsx               # VPS 列表（斑马纹）+ 全局财务汇总
    │   ├── providers/            # 提供商管理（page + ProviderManager 客户端）
    │   ├── customers/            # 客户列表（结算）+ [id] 详情（VPS/收款台账/差额/公开链接）
    │   └── vps/
    │       ├── vpsFormData.ts     # 表单类型与 toFormData（服务端可调用）
    │       ├── VpsForm.tsx        # VPS 新增/编辑表单（客户端）
    │       ├── new/page.tsx       # 新增 VPS
    │       └── [id]/
    │           ├── page.tsx       # 详情：续费历史 + 节点 + 编辑
    │           ├── RenewForm.tsx  # 续费表单（客户端）
    │           └── NodeManager.tsx# 节点增删改（客户端）
    └── api/
        ├── auth/login/route.ts        # POST 登录 / DELETE 退出
        ├── files/[name]/route.ts      # GET 读取上传截图（公开）
        └── admin/
            ├── upload/route.ts        # POST 上传付款截图 → data/uploads/
            ├── vps/[id]/balance/route.ts      # POST 更新余额/充值（仅 auto）
            ├── balance-logs/[id]/route.ts     # DELETE 余额/充值记录
            ├── vps/route.ts           # GET 列表 / POST 新增
            ├── vps/[id]/route.ts      # GET / PATCH / DELETE
            ├── vps/[id]/renew/route.ts# POST 续费
            ├── nodes/route.ts         # POST 新增节点
            └── nodes/[id]/route.ts    # PATCH / DELETE 节点
```

## 4. 鉴权设计（`src/lib/auth.ts`）

- **登录**：`POST /api/auth/login` 校验明文密码 `=== process.env.ADMIN_PASSWORD`（定长安全比较）。通过则用 `SESSION_SECRET` 对 payload 做 **HMAC-SHA256**，签名结果写入 httpOnly、SameSite=Lax 的 `vps_session` Cookie（有效期 7 天，生产环境 `secure`）。
- **验签**：中间件读取 Cookie，重新计算 HMAC 并定长比较。
- **退出**：`DELETE /api/auth/login` 将 Cookie `maxAge=0` 清除。
- **实现要点**：使用 **Web Crypto API**（`crypto.subtle`）而非 Node `crypto`，以便在 Next.js Edge 运行时（middleware）中可用，同时 Node 路由处理函数也兼容。
- 密码不入库，仅存环境变量；token 不含敏感信息，仅作"已认证"凭据。

## 5. 时间与状态计算（`src/lib/dates.ts`）

- `daysUntil(expiry)`：按自然日计算剩余天数（归零到当天 00:00，避免小时差偏差）。
- `expiryStatus(expiry)`：
  - `expired`（< 0 天）→ 红色徽章
  - `expiring`（0~7 天）→ 黄色徽章
  - `valid`（> 7 天）→ 绿色徽章
- 公开页与后台共用同一套工具，保证状态判断一致。

## 5.1 视觉风格与深色模式

- **设计基调**：现代简约 SaaS 风格，靛蓝（Indigo）主色，圆角卡片 + 柔和阴影 + 充足留白。
- **可复用样式**：在 `src/app/globals.css` 用 `@layer components` 集中定义 `.card` / `.input` / `.label` / `.btn-primary` / `.btn-success` / `.btn-secondary` / `.btn-danger` / `.btn-ghost` / `.badge` / `.table-head` / `.table-row`，每个都内置明暗两套配色。页面统一引用这些类，避免重复并保证一致。
- **深色模式**：Tailwind `darkMode: "class"`。
  - 防闪烁：`src/app/layout.tsx` 的 `<head>` 内联脚本在绘制前根据 `localStorage.theme` 或系统偏好给 `<html>` 加 `dark` 类。
  - 切换：`src/app/ThemeToggle.tsx`（客户端）切换 `dark` 类并写入 `localStorage`，放在后台导航、公开页、（登录页随系统）。
- **状态色**：`src/lib/dates.ts` 的 `statusBadgeClass` / `statusDotClass` 提供含深色变体的徽章与圆点颜色（绿/黄/红 = 有效/即将到期/已过期；蓝 = 自动续费）。
- **计费类型统一判定**：`src/lib/dates.ts` 的 `vpsValidity(vps)` 根据 `billingType` 返回 auto(「自动续费中」蓝色) 或 term(到期状态)，各页面统一调用，避免分散条件；auto 的 `expiryDate` 为 null，排序用 `nulls: "last"`。
- **默认暗色**：`layout.tsx` 防闪烁脚本无存储偏好时默认 `dark`。
- **导航布局**：后台保持顶部导航（粘性 + 毛玻璃背景）；公开页 `/view` 为单页卡片栅格。

## 6. 关键设计决策

- **不暴露全部客户**：不提供“展示所有客户”的全局公开页；裸 `/view` 重定向到登录，公开仅限 `/view/<客户ID>`（只读该客户数据）。客户专属页只取 `enabled: true` 的节点。
- **续费事务**：新增续费记录与更新 VPS 到期时间在 `prisma.$transaction` 中原子完成，避免数据不一致。
- **服务端/客户端边界**：纯函数 `toFormData` 单独放在 `vpsFormData.ts`（非 `"use client"`），避免被服务端组件当作客户端引用代理而无法调用。
- **财务汇总**：用 Prisma `aggregate` 分别对 VPS 表与续费表求和后相加，避免全量加载到内存。
- **复制 VPS**：不新增专用接口，复用新增流程——列表「复制」跳转 `/admin/vps/new?from=<id>`，新增页（服务端组件）读取该 VPS 并经 `toFormData` 预填表单（清空 id/IP、名称加"-副本"），用户修改后走普通 POST 创建。
- **删除续费记录**：`DELETE /api/admin/renewals/{id}` 仅删历史记录，刻意不联动修改 VPS 的 `expiryDate`，避免删除历史误改当前有效期；到期时间由编辑 VPS 显式控制。
- **付款/收款并排对账**：客户公开页把「购买 + 续费 + 充值」合并成一张付款台账，与收款台账左右并排。合并而非分表，是因为客户关心的是「我这些服务器一共花了多少」，而三类支出都计入同一个「总实际付款」口径；表尾合计与顶部卡片必须一致，否则客户对不上账。

## 7. 到期推送设计（`src/lib/notify.ts`）

- **触发方式：外部 cron 调用受保护端点**，而非进程内 `setInterval`。原因：应用以 pm2/systemd 单进程部署，进程重启会让内部定时器的时间点漂移，dev 模式也会误触发；外部 cron 的执行时间明确、可手动重跑、便于排查。端点 `/api/cron/expiry-notify` 刻意放在 middleware 匹配范围之外，自行用 `CRON_SECRET` 鉴权。
- **配置入库而非环境变量**：Bot Token、提前天数、站点地址、收件人都存数据库，改配置无需改 `.env` 和重启服务。其中**站点地址必须显式配置**——cron 从 `localhost` 调用，请求头里拿不到公网域名，无法自动推断客户专属链接。
- **收件人两层模型**：`NotifyRecipient.customerId` 有值 = 客户专属（只收自己服务器的提醒，在客户详情页配置）；为空 = 全局（通常是管理员，收所有客户的提醒，在设置页配置）。推送时取「该客户专属 + 全局」并按 chat_id 去重，两边都没配则跳过该客户。
- **按客户合并消息**：一个客户一条消息，链接就是其专属查看链接。若按服务器逐条推，同客户多台同时到期会重复刷屏，而客户点开的仍是同一个页面。
- **去重靠 `NotifyLog` 唯一键**而非「上次推送时间」字段：`@@unique([customerId, notifyDate])` 让重复调用天然幂等，且顺带留下可查的推送历史。后台「立即检查并推送」传 `force` 走 upsert 覆盖当天记录，用于验证配置。
- **两类计费统一判定**：`term` 用 `dates.ts` 的 `daysUntil`，`auto` 用 `billing.ts` 的 `estimateSharedBalance().daysRemaining`，不另起一套时间/余额计算，保证与页面展示口径一致。
- **发送容错**：逐个 chat_id 串行请求、10s 超时，单个失败只记录不抛出，避免一个失效 chat_id 让整轮推送中断。
