# Nginx 反向代理示例：把外网 80/443 转发到本地 Next.js 服务（127.0.0.1:$PORT）
# 将其放入 /etc/nginx/sites-available/ 并软链到 sites-enabled/，再 nginx -t && systemctl reload nginx

server {
    listen 80;
    server_name your-domain.com;

    # 如需 HTTPS，建议用 certbot 自动配置后此处会被改写为 443 + 证书
    location / {
        proxy_pass http://127.0.0.1:3000;   # 与 systemd 中的 PORT 保持一致
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
